import * as v2 from "firebase-functions/v2";

// export const helloworld = v2.https.onRequest((request, response) => {
//   const name = request.params[0];
//   const items = { lamp: "This is a lamp", chair: "Good chair" };
//   const message = items[name as keyof typeof items];

//   response.send(`<h1>${message}</h1>`);
// });

// TODO: Create cloud functions for various denormalisation, automation, etc.
